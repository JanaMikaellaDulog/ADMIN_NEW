# UI Modernization TODO (UI-only)

- [ ] Update `View/assets/style.css` with modern design tokens and consistent typography/shadows/hover/animations.
- [ ] Fix responsive layout issues in `View/assets/style.css` (remove cutting from fixed/hidden overflow; ensure proper scrolling on tablet/mobile).
- [x] Improve sidebar collapsible UI purely via CSS (smooth transitions, better spacing, smaller elements), matching existing JS toggle behavior.

- [ ] Modernize topbar branding in `View/PHP/admin.php` (Imperial Homes Corporation) with better typography + responsive wrapping.
- [ ] Align login branding style with the updated theme (`View/PHP/login.php` + `View/assets/login_style.css` if needed).
- [ ] Verify modals/tables/charts aren’t cut off across desktop/tablet/mobile.

